using Verse;

namespace SlippersNmSpc;

public class CompProperties_TerrainDetector : CompProperties
{
    public CompProperties_TerrainDetector()
    {
        compClass = typeof(CompTerrainDetector);
    }
}
